use std::fs;
mod data_processing;

use crate::data_processing::conversation_cleaner;
fn main() {
    // SELECT JSON FILE
    // SERIALIZE JSON FILE
    // REFINE AND REMOVE UNECESSARY DATA
    // PUSH TO DATABASE
}